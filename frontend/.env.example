# TripMate AI — Frontend Environment Configuration Example
# Public variables only (prefixed with VITE_). No secrets.

VITE_API_URL=http://localhost:8000
VITE_APP_NAME=TripMate AI
VITE_GEOCODING_API_URL=https://geocoding-api.open-meteo.com/v1/search
